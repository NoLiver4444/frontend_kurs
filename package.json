{
  "name": "tournament-brackets-app",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  }
,
  "devDependencies": {
    "eslint": "^8.0.0",
    "gh-pages": "^6.3.0",
    "prettier": "^3.0.0",
    "vite": "^5.0.0"
  }
}
